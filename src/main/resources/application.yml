spring:
  application:
    name: basketball
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/basketball}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:artem201320133}
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/main.yaml

server:
  port: 8040

logging:
  level:
    root: info
    liquibase: debug
    org.springframework.security: DEBUG

springdoc:
  swagger-ui:
    urls:
      - name: general
        url: /oas/basketball.yaml

token:
  signing:
    key: ${JWT_SIGNING_KEY:tXKjU8nJ7vWyQf9ZpTeR5mBL0hCuC23HZUTMZEMdYAw=}
